[x] API gateway needs to pass custom header with HMAC
[ ] refactor hmac check as authorization Lambda -> cannot, since authz lambda does not receive body
[ ] split policy evaluation into separate Lambda
[ ] arm64 lambda? also needs to recompilation
